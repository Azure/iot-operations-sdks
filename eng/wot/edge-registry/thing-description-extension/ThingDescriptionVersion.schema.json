{
  "$schema": "https://json-schema.org/draft-07/schema",
  "title": "ThingDescriptionVersion",
  "description": "A specific version of a thing description.",
  "type": "object",
  "additionalProperties": false,
  "required": [ "extensions", "version"],
  "properties": {
    "version": {
      "description": "The generic version entity.",
      "$ref": "../core-xregistry/Version.schema.json"
    },
    "extensions": {
      "description": "Additional fields and field guarantees for the thing description extension.",
      "$ref": "ThingDescriptionVersionExtensions.schema.json"
    }
  }
}

{
  "$schema": "https://json-schema.org/draft-07/schema",
  "title": "CreateThingDescriptionAttributes",
  "description": "Request payload for creating a thing description with an initial version.",
  "type": "object",
  "additionalProperties": false,
  "required": [ "thingDescriptionMetaAttributes", "createThingDescriptionVersionAttributes" ],
  "properties": {
    "thingDescriptionMetaAttributes": {
      "description": "Initial meta attributes for the thing description.",
      "$ref": "../core-xregistry/ResourceMetaAttributes.schema.json"
    },
    "createThingDescriptionVersionAttributes": {
      "description": "Initial attributes for creating a Thing Description version.",
      "$ref": "CreateThingDescriptionVersionAttributes.schema.json"
    }
  }
}

{
    "$schema": "https://json-schema.org/draft-07/schema",
    "title": "CreateThingDescriptionVersionAttributes",
    "description": "Request payload for creating a Thing Description Version.",
    "type": "object",
    "additionalProperties": false,
    "required": ["labels", "thingDescriptionDocument", "thingDescriptionLabels"],
    "properties": {
        "groupId": {
            "description": "Group identifier for this Version.",
            "type": "string"
        },
        "name": {
            "description": "Human-readable name.",
            "type": "string"
        },
        "description": {
            "description": "A human-readable summary of the purpose of the entity.",
            "type": "string"
        },
        "documentation": {
            "description": "A URL to additional information about this entity.",
            "type": "string"
        },
        "labels": {
            "description": "Queryable Key Value pairs to be added to the Version. Labels can be used to store additional metadata about the Version.",
            "type": "array",
            "items": {
                "type": "object",
                "$ref": "../core-xregistry/Label.schema.json"
            }
        },
        "ancestor": {
            "description": "The versionId of this Version's ancestor if it has an ancestor.",
            "type": "string"
        },
        "contentType": {
            "description": "Content type of the thing description Version.",
            "type": "string"
        },
        "format": {
            "description": "Thing description format identifier, e.g. `WoT-TD/1.1`.",
            "type": "string"
        },
        "thingDescriptionDocument": {
            "type": "string",
            "description": "Base64-encoded thing description document content.",
            "contentEncoding": "base64"
        },
        "thingDescriptionLabels": {
            "description": "Queryable Key Value pairs to be added to the parent Thing Description. Labels can be used to store additional metadata about the Thing Description.",
            "type": "array",
            "items": {
                "type": "object",
                "$ref": "../core-xregistry/Label.schema.json"
            }
        }
    }
}

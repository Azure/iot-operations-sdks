{
  "$schema": "https://json-schema.org/draft-07/schema",
  "type": "object",
  "title": "ThingDescriptionVersionExtensions",
  "description": "Additional fields and field guarantees for the thing description extension.",
  "required": [ "thingDescriptionDocument" ],
  "properties": {
    "contentType": {
      "description": "Content type of the thing description version.",
      "type": "string"
    },
    "format": {
      "description": "Thing description format identifier, e.g. `WoT-TD/1.1`.",
      "type": "string"
    },
    "thingDescriptionDocument": {
      "description": "The raw thing description document for this version as bytes.",
      "type": "string",
      "contentEncoding": "base64"
    }
  }
}
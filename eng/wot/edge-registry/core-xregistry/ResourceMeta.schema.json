{
  "$schema": "https://json-schema.org/draft-07/schema",
  "title": "ResourceMeta",
  "type": "object",
  "additionalProperties": false,
  "required": [ "id", "xid", "epoch", "defaultVersionId", "defaultVersionSticky", "readOnly", "createdAt", "modifiedAt", "extensions", "labels" ],
  "properties": {
    "compatibility": {
      "description": "States that Versions of this Resource adhere to a certain compatibility rule.",
      "type": "string"
    },
    "createdAt": {
      "description": "The date/time of when the entity was created.",
      "type": "string", "format": "date-time"
    },
    "defaultVersionId": {
      "description": "The versionId of the default Version of the Resource.",
      "type": "string"
    },
    "defaultVersionSticky": {
      "description": "A value of true means that `defaultVersionId` has been explicitly set and MUST NOT automatically change if other Versions are added or removed. A value of false means the default Version MUST be the newest Version, as defined by the Resource's versionmode algorithm.",
      "type": "boolean"
    },
    "deprecated": {
      "description": "Information about deprecation status of the entity, if applicable.",
      "$ref": "DeprecatedInfo.schema.json"
    },
    "epoch": {
      "description": "A numeric value used to determine whether an entity has been modified.",
      "type": "integer"
    },
    "extensions": {
      "description": "Extension-specific attributes (e.g., `format` and `content_type` for schemas).",
      "type": "object", "additionalProperties": { "type": "string", 
      "contentEncoding": "base64" }
    },
    "labels": {
      "description": "A mechanism in which additional metadata about the entity can be stored without changing the model definition of the entity. Labels can be used for querying.",
      "type": "array",
      "items": {
          "type": "object",
          "$ref": "Label.schema.json"
      }
    },
    "id": {
      "description": "Resource identifier.",
      "type": "string"
    },
    "modifiedAt": {
      "description": "The date/time of when the entity was last updated.",
      "type": "string", "format": "date-time"
    },
    "readOnly": {
      "description": "Indicates whether the Resource is updateable by clients.",
      "type": "boolean"
    },
    "xid": {
      "description": "Full XID path.",
      "type": "string"
    },
    "xref": {
      "description": "Indicates that this Resource is a reference to another Resource within the same Registry. The XID path of the referenced Resource.",
      "type": "string"
    }
  }
}

{
  "$schema": "https://json-schema.org/draft-07/schema",
  "title": "DeleteRequestPayload",
  "description": "Request payload for deleting an xRegistry entity.",
  "type": "object",
  "additionalProperties": false,
  "required": [],
  "properties": {
    "groupId": {
      "description": "Group identifier. Uses the default if not specified.",
      "type": "string"
    },
    "options": {
      "description": "Options that control the behavior of the delete operation.",
      "$ref": "DeleteOptions.schema.json"
    }
  }
}

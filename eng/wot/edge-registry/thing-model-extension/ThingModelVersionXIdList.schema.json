{
  "$schema": "https://json-schema.org/draft-07/schema",
  "title": "ThingModelVersionXIdList",
  "description": "List of Thing Model Version XIDs within the provided constraints with the specified label.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "versions"
  ],
  "properties": {
    "versions": {
      "description": "List of Thing Model Version XID components.",
      "type": "array",
      "items": {
        "$ref": "ThingModelVersionXId.schema.json"
      }
    }
  }
}

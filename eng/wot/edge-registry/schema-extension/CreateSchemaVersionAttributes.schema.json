{
    "$schema": "https://json-schema.org/draft-07/schema",
    "title": "CreateSchemaVersionAttributes",
    "description": "Request payload for creating a Schema Version.",
    "type": "object",
    "additionalProperties": false,
    "required": [
        "labels",
        "schemaDocument",
        "schemaLabels"
    ],
    "properties": {
        "groupId": {
            "description": "Group identifier for this Version.",
            "type": "string"
        },
        "name": {
            "description": "Human-readable name.",
            "type": "string"
        },
        "description": {
            "description": "A human-readable summary of the purpose of the entity.",
            "type": "string"
        },
        "documentation": {
            "description": "A URL to additional information about this entity.",
            "type": "string"
        },
        "labels": {
            "description": "Queryable Key Value pairs to be added to the Version. Labels can be used to store additional metadata about the Version.",
            "type": "array",
            "items": {
                "type": "object",
                "$ref": "../core-xregistry/Label.schema.json"
            }
        },
        "ancestor": {
            "description": "The versionId of this Version's ancestor if it has an ancestor.",
            "type": "integer"
        },
        "contentType": {
            "description": "Content type of the schema Version.",
            "type": "string"
        },
        "format": {
            "description": "Schema format identifier, e.g. `JsonSchema/draft-07`, `Protobuf/3`.",
            "type": "string"
        },
        "schemaLabels": {
            "description": "Queryable Key Value pairs to be added to the parent Schema. Labels can be used to store additional metadata about the Schema.",
            "type": "array",
            "items": {
                "type": "object",
                "$ref": "../core-xregistry/Label.schema.json"
            }
        },
        "schemaDocument": {
            "type": "string",
            "description": "Base64-encoded schema document content.",
            "contentEncoding": "base64"
        }
    }
}
